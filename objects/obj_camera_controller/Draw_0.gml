/// @description Applies the camera view for rendering.
// This ensures that the game world is drawn according to the camera's
// position, zoom, and other settings. It's crucial for the camera to work.


camera_apply(view_camera[0]);